# 🦖 Deno Extras

Extra wrappers for Deno [runtime API](https://doc.deno.land/builtin/stable) and [standard library](https://deno.land/std).

## Usage

```ts
import { readJsonSync } from 'https://deno.land/x/deno_extras@v0.1.0/mod.ts'

const config = readJsonSync('./config.json')
```
